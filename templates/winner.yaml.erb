---
robot_jid: <%= robot_jid %>
to_jid: <%= to_jid %>
account_id: <%= account_id %>
content:
  header:
    text: "Lottery Shares"
  body:
    - type: "section"
      sections:
        - type: "message"
          text: "Call on <%= person.name %>"
        - type: "actions"
          items:
            - text: "They started sharing"
              value: "mark-sharing:<%= person.meeting_id %>.<%= person.id %>"
              style: 'Primary'
            - text: "Draw again"
              value: "draw-again:<%= person.meeting_id %>"
              style: "Danger"
  footer: "Winner winner chicken dinner!"
